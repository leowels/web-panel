# ============================================
# ВСЕ ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ ДЛЯ TIMEWEB CLOUD
# ============================================

# ============================================
# ОБЯЗАТЕЛЬНЫЕ ПЕРЕМЕННЫЕ
# ============================================

# Секретный ключ для JWT (ОБЯЗАТЕЛЬНО!)
SECRET_KEY=8E7ExQa6dqn-yERIMMHbA_IzLdvTJ6Vw1mLSslMoBp0

# PostgreSQL настройки (ОБЯЗАТЕЛЬНО!)
POSTGRESQL_HOST=192.168.0.4
POSTGRESQL_PORT=5432
POSTGRESQL_USER=gen_user
POSTGRESQL_PASSWORD=n+qIlo?#l<v5Zh
POSTGRESQL_DBNAME=default_db
POSTGRESQL_SSL=true

# ============================================
# ОПЦИОНАЛЬНЫЕ ПЕРЕМЕННЫЕ (с значениями по умолчанию)
# ============================================

# Порты (можно не указывать, используются значения по умолчанию)
BACKEND_PORT=8000
FRONTEND_PORT=3000

# Окружение
ENVIRONMENT=production

# CORS - ОБЯЗАТЕЛЬНО укажите ваш домен!
CORS_ORIGINS=https://leowels-web-panel-e5fe.twc1.net,http://localhost:3000

# Админ пароль (если не указан, используется "admin123")
ADMIN_PASSWORD=admin123
ADMIN_FORCE_RESET_PASSWORD=true
# AI настройки (опционально, если используете AI)
AI_API_URL=ваш_ai_api_url
AI_API_KEY=ваш_ai_api_key
AI_HTTP_TIMEOUT_SECONDS=45

# ============================================
# BUILD ARGUMENTS (для сборки Docker образа)
# ============================================
# Эти переменные указываются как Build Arguments, а не Environment Variables!

NEXT_PUBLIC_API_URL=http://localhost:8000
BACKEND_URL=http://localhost:8000

# ============================================
# ИНСТРУКЦИЯ
# ============================================
# 
# 1. Скопируйте все переменные из раздела "ОБЯЗАТЕЛЬНЫЕ" и "ОПЦИОНАЛЬНЫЕ"
# 2. Замените значения на ваши реальные:
#    - POSTGRESQL_PASSWORD - ваш пароль от БД
#    - CORS_ORIGINS - ваш домен (замените leowels-web-panel-e5fe.twc1.net)
#    - ADMIN_PASSWORD - ваш пароль для админа
# 3. Build Arguments указываются отдельно при создании приложения
# 4. В Timeweb Cloud основной порт должен быть 3000 (не 8000!)




ENABLE_TELEGRAM_INGEST=false

TELEGRAM_INGEST_TOKEN=replace_with_strong_random_token
