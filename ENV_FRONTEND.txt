# Frontend variables for the single Docker container deployment.
# For Timeweb Cloud leave API URL empty so browser requests go to /api/*
# and Next.js proxies them to the backend inside the same container.

NEXT_PUBLIC_API_URL=
BACKEND_URL=http://127.0.0.1:8000
NODE_ENV=production
PORT=3000
NEXT_TELEMETRY_DISABLED=1
