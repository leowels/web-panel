# Исправление видимости вкладки "Карта цеха"

## ✅ Что проверено:
- ✅ Вкладка добавлена в `components/Sidebar.tsx` (строка 23)
- ✅ Вкладка добавлена в `components/MobileMenu.tsx` (строка 25)
- ✅ Страница существует: `app/workshop-map/page.tsx`

## 🔧 Решения:

### 1. Перезапуск Frontend (если используете dev режим)

Если запускаете через `npm run dev`:
```bash
# Остановите сервер (Ctrl+C)
# Затем запустите снова:
npm run dev
```

### 2. Пересборка Frontend (если используете Docker)

```bash
# Пересоберите Docker образ
docker build -t web-panel:latest .

# Или перезапустите контейнер
docker restart web-panel
```

### 3. Очистка кэша Next.js

```bash
# Удалите папку .next
rm -rf .next

# Или в Windows PowerShell:
Remove-Item -Recurse -Force .next

# Затем пересоберите:
npm run build
```

### 4. Очистка кэша браузера

- Нажмите `Ctrl + Shift + R` (жесткая перезагрузка)
- Или `Ctrl + F5`
- Или откройте в режиме инкогнито

### 5. Проверка напрямую

Попробуйте открыть страницу напрямую по URL:
```
http://localhost:3000/workshop-map
```

### 6. Проверка консоли браузера

Откройте DevTools (F12) и проверьте:
- Нет ли ошибок в консоли
- Нет ли ошибок 404 для `/workshop-map`

## 🔍 Если ничего не помогло:

Проверьте, что файлы действительно сохранены:

1. `components/Sidebar.tsx` - должна быть строка с "Карта цеха"
2. `app/workshop-map/page.tsx` - должен существовать файл

Если используете Docker, убедитесь, что файлы скопированы в контейнер.
